[wokwi]
version = 1
firmware = '.pio/build/esp32dev/firmware.bin'
elf = '.pio/build/esp32dev/firmware.elf'

# Para una fase posterior con servidor web en el ESP32 virtual:
# [[net.forward]]
# from = 'localhost:8180'
# to = 'target:80'
